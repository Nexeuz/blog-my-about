import { FirestoreTranslateLoader } from './firestore-translate-loader';

describe('FirestoreTranslateLoader', () => {
  it('should create an instance', () => {
    expect(new FirestoreTranslateLoader()).toBeTruthy();
  });
});
